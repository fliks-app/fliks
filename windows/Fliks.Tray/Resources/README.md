# Tray icon

Drop a `fliks.ico` here (multi-resolution: 16/32/48/256 px). It becomes the
executable icon (`ApplicationIcon`) and the `NotifyIcon` in the system tray.

If absent, the build still succeeds and the tray falls back to the default
system application icon at runtime.
