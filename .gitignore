# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Tests and coverage
coverage/
.nyc_output/

# Caches
.cache/
.turbo/
.parcel-cache/
.angular/

# IDE and editors
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project-specific
# Android signing keystore — never commit. Local copy lives outside
# the repo (1Password / etc.); CI receives it as a base64-encoded
# secret decoded at build time.
client/android/app/fliks-upload.jks

# Tizen / webOS signing material — never commit. Each contributor uses
# their own Samsung-issued distributor cert (tied to a TV DUID) stored
# outside the repo (~/tizen-studio-data/). CI uses an org-level secret.
*.p12
*.pwd
client/tizen-studio-cli.bin
client/webos/*.ipk

# Store listing assets are versioned (see store/README.md), signed packages are
# not: a signature embeds the author certificate and this repository is public.
store/**/*.wgt
store/**/*.ipk
store/**/*.ipa
volumes/*
/backend/images/
/scripts/
logos/
plans/
# Scratch documentation stays local; the plugin contract is shared, so it is tracked.
/docs/*
!/docs/plugins.md
docker-compose.yml
docker-compose.prod.yml
.worktrees/
.claude/

release/

# macOS native app — vendored binaries and build artifacts
macos/Vendored/
macos/build/
macos/.build/
macos/*.xcodeproj/xcuserdata/
macos/*.xcodeproj/project.xcworkspace/xcuserdata/
